import { describe, expect, test } from 'vitest';
import { render, screen } from '@testing-library/react';
import TimelineItem from './timeline-item';

describe('TimelineItem 렌더', () => {
  test('TimelineItem 렌더', () => {});
});
